/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/srhashdh_Blog',
  assetPrefix: '/srhashdh_Blog/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;